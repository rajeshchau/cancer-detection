export const dynamic = 'force-dynamic';
export const runtime = 'nodejs';

// This file ensures all API routes under /app/api use dynamic rendering
// and the Node.js runtime instead of Edge by default

export default function ApiRoutesConfig() {
  return null;
}